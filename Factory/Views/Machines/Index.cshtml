@{
  Layout = "_Layout";
}

<h1>Machine Index</h1>
@using Factory.Models
@if(@Model.Count == 0)
{
  <li>No Machines have been added</li>
}

<ul>
@foreach(Machine machine in Model)
{
  <li>@Html.ActionLink($"{machine.Name}","Details", new { id = machine.MachineId})</li>
}
</ul>

<p class="enlarge">@Html.ActionLink("Add Machine", "Create")</p>
<p class="enlarge">@Html.ActionLink("Main Menu", "Index", "Home")</p>